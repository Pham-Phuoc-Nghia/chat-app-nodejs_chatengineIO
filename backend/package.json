{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  },
  "scripts": {
    "build": "(npm install && npm install express cors axios && npm install --save-dev nodemon) && (cd ../frontend && npm install && npm run dev)",
    "start": "nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "nodemon": "^3.1.0"
  }
}
